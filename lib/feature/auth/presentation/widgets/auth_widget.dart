import 'package:flutter/material.dart';

/// Widget for auth

class AuthWidget extends StatelessWidget {
  const AuthWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // Add your UI here
      child: const Text('auth Widget'),
    );
  }
}
