import 'package:flutter/material.dart';

/// Widget for home

class HomeWidget extends StatelessWidget {
  const HomeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // Add your UI here
      child: const Text('home Widget'),
    );
  }
}
